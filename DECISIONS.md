# DECISIONS.md — دفترچه تصمیمات معماری (ADR)

این فایل تصمیمات معماری و فنی مهم پروژه را ثبت می‌کند تا دلیل انتخاب‌ها در آینده قابل پیگیری باشد.

قالب هر تصمیم:

```text
ADR-XXXX — عنوان تصمیم
وضعیت: Proposed | Accepted | Deprecated | Superseded
تاریخ: YYYY-MM-DD
زمینه
تصمیم
پیامدها
```

---

## فهرست تصمیمات

| شناسه | عنوان | وضعیت |
|---|---|---|
| [ADR-0001](#adr-0001--استفاده-از-php-ساده-و-vanilla-javascript) | استفاده از PHP ساده و Vanilla JavaScript | Accepted |
| [ADR-0002](#adr-0002--نگهداری-کاتالوگ-قطعات-در-mysqlmariadb) | نگهداری کاتالوگ قطعات در MySQL/MariaDB | Accepted |
| [ADR-0003](#adr-0003--دریافت-مرحله‌ای-دادهها-بر-اساس-شاسی) | دریافت مرحله‌ای داده‌ها بر اساس شاسی | Accepted |
| [ADR-0004](#adr-0004--استفاده-از-ستونهای-json-برای-قوانین-سازگاری) | استفاده از ستون‌های JSON برای قوانین سازگاری | Accepted |
| [ADR-0005](#adr-0005--اعتبارسنجی-دو-لایه-در-فرانتاند-و-بکاند) | اعتبارسنجی دو لایه در فرانت‌اند و بک‌اند | Accepted |
| [ADR-0006](#adr-0006--ذخیره-کانفیگهای-ناتمام-در-localstorage) | ذخیره کانفیگ‌های ناتمام در localStorage | Accepted |
| [ADR-0007](#adr-0007--حذف-tailwind-cdn-و-استفاده-از-css-لوکال) | حذف Tailwind CDN و استفاده از CSS لوکال | Accepted |
| [ADR-0008](#adr-0008--محاسبه-ظرفیت-قابل-استفاده-storage-بر-اساس-raid) | محاسبه ظرفیت قابل استفاده Storage بر اساس RAID | Accepted |
| [ADR-0009](#adr-0009--ثبت-پیشفاکتور-با-کد-رهگیری-مستقل) | ثبت پیش‌فاکتور با کد رهگیری مستقل | Accepted |
| [ADR-0010](#adr-0010--تفکیک-آینده-فایل-mainjs-به-ماژولهای-کوچکتر) | تفکیک آینده فایل `main.js` به ماژول‌های کوچک‌تر | Proposed |
| [ADR-0011](#adr-0011--انتقال-تنظیمات-اتصال-دیتابیس-به-config-امن) | انتقال تنظیمات اتصال دیتابیس به Config امن | Proposed |
| [ADR-0012](#adr-0012--تکمیل-موتور-پیشنهاددهی-مسیر-راهنمایی) | تکمیل موتور پیشنهاددهی مسیر راهنمایی | Proposed |
| [ADR-0013](#adr-0013--استفاده-از-جدول-سرورهای-آماده-برای-پیشنهادها) | استفاده از جدول سرورهای آماده برای پیشنهادها | Accepted |

---

## ADR-0001 — استفاده از PHP ساده و Vanilla JavaScript

**وضعیت:** Accepted
**تاریخ:** 2026-09-05

### زمینه

پروژه کوچک، متمرکز و بدون نیاز فوری به Framework سمت سرور یا کلاینت است. هدف فعلی اجرای سریع کانفیگوراتور و اعتبارسنجی اولیه سخت‌افزاری است.

### تصمیم

اپلیکیشن با `index.php`، چند API ساده PHP و فایل `assets/main.js` پیاده‌سازی می‌شود؛ بدون React/Vue/Laravel یا ابزار Build.

### پیامدها

**مزایا:**

- راه‌اندازی سریع و وابستگی کم.
- استقرار ساده روی هاست‌های PHP رایج.
- Debug مستقیم بدون Pipeline پیچیده.

**معایب:**

- با بزرگ شدن پروژه، نگهداری فایل‌های بزرگ سخت می‌شود.
- نبود Component System و Type Safety احتمال خطای UI/State را بالا می‌برد.
- نیاز جدی به ماژولار کردن در فازهای بعدی.

---

## ADR-0002 — نگهداری کاتالوگ قطعات در MySQL/MariaDB

**وضعیت:** Accepted
**تاریخ:** 2026-09-05

### زمینه

کاتالوگ قطعات شامل شاسی، CPU، RAM، Storage، PSU، GPU، Network Adapter، Riser، HBA و Optical Drive است. این داده‌ها رابطه‌مند هستند و باید قابل Query و فیلتر باشند.

### تصمیم

داده‌های قطعات در MySQL/MariaDB نگهداری می‌شوند و فایل `falnicc1_server_configurator.sql` اسکیمای اولیه و Seed Data را فراهم می‌کند.

### پیامدها

**مزایا:**

- Query ساده با SQL و PDO.
- امکان توسعه به پنل مدیریت قیمت/قطعات.
- مناسب برای هاست‌های معمول PHP/MySQL.

**معایب:**

- قوانین پیچیده‌تر سخت‌افزاری ممکن است فقط با SQL ساده قابل مدل‌سازی نباشند.
- نیازمند Migration منظم در آینده است.

---

## ADR-0003 — دریافت مرحله‌ای داده‌ها بر اساس شاسی

**وضعیت:** Accepted
**تاریخ:** 2026-09-05

### زمینه

همه قطعات با همه شاسی‌ها سازگار نیستند. بارگذاری کل کاتالوگ در شروع اپلیکیشن می‌تواند حجم پاسخ اولیه را زیاد کند و فیلترهای سمت کلاینت را پیچیده‌تر کند.

### تصمیم

`GET api/get_data.php` در حالت بدون `chassis_id` فقط لیست شاسی‌ها را می‌دهد. پس از انتخاب شاسی، همان API با `chassis_id` قطعات سازگار با شاسی را برمی‌گرداند.

### پیامدها

**مزایا:**

- کاهش حجم داده اولیه.
- ساده‌تر شدن فهرست‌های انتخاب.
- جداسازی طبیعی مرحله انتخاب شاسی از قطعات وابسته.

**معایب:**

- تغییر شاسی نیازمند Reset شدن بخشی از State انتخاب‌شده است.
- وابستگی فرانت‌اند به ترتیب صحیح Load داده‌ها بیشتر می‌شود.

---

## ADR-0004 — استفاده از ستون‌های JSON برای قوانین سازگاری

**وضعیت:** Accepted
**تاریخ:** 2026-09-05

### زمینه

بعضی قوانین مثل `storage_rules`، `cooling_rules`، `compatible_chassis_ids` و `compatible_cpu_ids` ساختاری منعطف‌تر از ستون‌های ساده دارند.

### تصمیم

برای قوانین منعطف از ستون‌های JSON استفاده می‌شود. API پس از Query، فیلدهای JSON را Decode می‌کند تا در فرانت‌اند به شکل Object/Array استفاده شوند.

### پیامدها

**مزایا:**

- انعطاف در افزودن Ruleهای جدید بدون تغییر فوری اسکیمای اصلی.
- خوانایی بهتر برای قوانین شاسی و سازگاری.

**معایب:**

- Query روی JSON پیچیده‌تر و گاهی کندتر از جدول‌های رابطه‌ای نرمال‌شده است.
- اعتبارسنجی ساختار JSON باید جدی گرفته شود.
- برای Ruleهای بسیار پیچیده، احتمالاً نیاز به Rule Engine یا جدول‌های اختصاصی خواهد بود.

---

## ADR-0005 — اعتبارسنجی دو لایه در فرانت‌اند و بک‌اند

**وضعیت:** Accepted
**تاریخ:** 2026-09-05

### زمینه

کاربر باید هنگام انتخاب قطعات، سریع و واضح خطاها را ببیند. اما داده ارسالی از مرورگر قابل اعتماد نیست.

### تصمیم

- فرانت‌اند در `validator.runChecks()` بازخورد لحظه‌ای می‌دهد.
- بک‌اند در `submit_config.php` قبل از ثبت کانفیگ، قطعات را دوباره از دیتابیس می‌خواند و اعتبارسنجی می‌کند.

### پیامدها

**مزایا:**

- UX بهتر و کاهش خطاهای کاربر.
- امنیت و صحت بیشتر هنگام ثبت نهایی.

**معایب:**

- احتمال Divergence بین قوانین فرانت‌اند و بک‌اند وجود دارد.
- هر Rule مهم باید در هر دو لایه یا در یک منبع مشترک پیاده‌سازی شود.

---

## ADR-0006 — ذخیره کانفیگ‌های ناتمام در localStorage

**وضعیت:** Accepted
**تاریخ:** 2026-09-05

### زمینه

کاربر ممکن است فرایند کانفیگ را نیمه‌کاره رها کند. برای تجربه بهتر، سیستم باید امکان ادامه کانفیگ را فراهم کند.

### تصمیم

Draftها در `localStorage` مرورگر با کلید `falnic_server_sessions` ذخیره می‌شوند. کانفیگ‌های Completed نیز برای مشاهده مجدد در همان مرورگر نگهداری می‌شوند.

### پیامدها

**مزایا:**

- پیاده‌سازی ساده بدون نیاز به Login.
- بازیابی سریع روی همان مرورگر.

**معایب:**

- اطلاعات بین دستگاه‌ها Sync نمی‌شود.
- پاک شدن Cache/Storage مرورگر باعث حذف Draftها می‌شود.
- برای استفاده سازمانی، ذخیره سمت سرور بهتر است.

---

## ADR-0007 — حذف Tailwind CDN و استفاده از CSS لوکال

**وضعیت:** Accepted
**تاریخ:** 2026-09-05

### زمینه

پروژه در ابتدا برای سرعت توسعه از Tailwind CDN و بخشی CSS داخل `index.php` استفاده می‌کرد. نیاز جدید این است که تمام وابستگی‌های خارجی UI حذف شوند و ظاهر فعلی بدون تغییر با فایل‌های لوکال حفظ شود.

### تصمیم

- لینک Tailwind CDN از `index.php` حذف شد.
- استایل‌های مورد نیاز به صورت Utility CSS و قواعد اختصاصی در `assets/style.css` پیاده‌سازی شد.
- فونت اصلی از مسیر لوکال `assets/falnic-font.woff2` با `@font-face` خوانده می‌شود.
- لوگوی HPE از مسیر لوکال `assets/hpe-logo.svg` استفاده می‌شود.

### پیامدها

**مزایا:**

- حذف وابستگی خارجی برای رندر UI.
- کنترل کامل روی CSS و فونت.
- پایداری بیشتر در محیط‌هایی که دسترسی CDN محدود است.

**معایب:**

- برای هر کلاس جدید باید Utility متناظر در `assets/style.css` اضافه شود.
- نگهداری دستی Utilityها نسبت به Build خودکار Tailwind نیازمند دقت بیشتر است.

---

## ADR-0008 — محاسبه ظرفیت قابل استفاده Storage بر اساس RAID

**وضعیت:** Accepted
**تاریخ:** 2026-09-05

### زمینه

ظرفیت خام دیسک‌ها با ظرفیت قابل استفاده بعد از RAID یکی نیست و کاربر باید ظرفیت واقعی تقریبی را ببیند.

### تصمیم

فرانت‌اند ظرفیت قابل استفاده را بر اساس RAIDهای رایج محاسبه می‌کند:

| RAID | قاعده ظرفیت قابل استفاده |
|---|---|
| JBOD/None | `qty * capacity` |
| RAID 1 | `capacity` برای 2 دیسک |
| RAID 5 | `(qty - 1) * capacity` |
| RAID 6 | `(qty - 2) * capacity` |
| RAID 10 | `(qty / 2) * capacity` |
| RAID 50 | `(qty - 2) * capacity` |
| RAID 60 | `(qty - 4) * capacity` |

### پیامدها

**مزایا:**

- نمایش بهتر ظرفیت واقعی برای تصمیم‌گیری کاربر.
- امکان مقایسه سریع بین RAIDها.

**معایب:**

- محاسبات ساده‌سازی شده‌اند و همه جزئیات Vendor/Controller را پوشش نمی‌دهند.
- بک‌اند هنوز باید این منطق را کامل‌تر بازسازی کند اگر ظرفیت در ثبت نهایی مبنای تصمیم باشد.

---

## ADR-0009 — ثبت پیش‌فاکتور با کد رهگیری مستقل

**وضعیت:** Accepted
**تاریخ:** 2026-09-05

### زمینه

پس از نهایی شدن کانفیگ، کاربر نیاز به کد پیگیری دارد تا بتواند درخواست پیش‌فاکتور را دنبال کند.

### تصمیم

در `submit_config.php` یک کد رهگیری با قالب `HPE-XXXXXX` تولید و در ستون `session_id` جدول `User_Configurations` ذخیره می‌شود.

### پیامدها

**مزایا:**

- کد کوتاه و قابل نمایش به کاربر.
- عدم وابستگی به ID داخلی دیتابیس در UI.

**معایب:**

- نام ستون `session_id` از نظر معنایی دقیق نیست؛ در عمل نقش Tracking Code را دارد.
- بهتر است در آینده ستون مجزای `tracking_code` اضافه شود.

---

## ADR-0010 — تفکیک آینده فایل `main.js` به ماژول‌های کوچک‌تر

**وضعیت:** Proposed
**تاریخ:** 2026-09-05

### زمینه

فایل `assets/main.js` تمام منطق State، API، Wizard، Validator، Renderer و Configurator را در خود دارد و بزرگ شده است.

### تصمیم پیشنهادی

در فاز بعدی فایل به ماژول‌های کوچک‌تر تقسیم شود:

```text
assets/js/
├── state.js
├── api.js
├── session-manager.js
├── validators.js
├── pcie.js
├── pro-wizard.js
├── guidance-wizard.js
├── configurator.js
└── invoice-renderer.js
```

### پیامدها

**مزایا:**

- نگهداری ساده‌تر.
- تست‌پذیری بهتر.
- کاهش ریسک تغییرات ناخواسته.

**معایب:**

- نیازمند تصمیم درباره Module Loading یا Build Tool است.
- برای هاست ساده PHP باید روش Load فایل‌ها دقیق انتخاب شود.

---

## ADR-0011 — انتقال تنظیمات اتصال دیتابیس به Config امن

**وضعیت:** Accepted
**تاریخ:** 2026-09-06

### زمینه

اتصال دیتابیس در فایل‌های API به صورت مستقیم تعریف شده است. این روش برای Production مناسب نیست و باعث افشای Secret در Repository می‌شود.

### تصمیم پیشنهادی

- ساخت `config/database.php` به عنوان تنها نقطه ساخت PDO.
- نگهداری Override واقعی در `config/database.local.php` که در Git Ignore شده است.
- ارائه نمونه قابل کپی در `config/database.local.example.php`.
- پشتیبانی از Environment Variableها:
  - `DB_HOST`
  - `DB_NAME`
  - `DB_USER`
  - `DB_PASS`
  - `DB_CHARSET`

### پیامدها

**مزایا:**

- امنیت بهتر.
- امکان تنظیم محیط‌های Development/Staging/Production بدون تغییر کد.
- کاهش احتمال Leak شدن Credential.

**معایب:**

- نیاز به مستندسازی نصب و Deployment.
- ممکن است هاست‌های اشتراکی محدودیت Environment Variable داشته باشند.

---

## ADR-0012 — تکمیل موتور پیشنهاددهی مسیر راهنمایی

**وضعیت:** Proposed
**تاریخ:** 2026-09-05

### زمینه

مسیر راهنمایی اکنون سؤال‌های خوبی دارد اما خروجی پیشنهادها عمدتاً ثابت/دمو است. محصول برای ارزش واقعی باید پاسخ‌ها را به کانفیگ‌های قابل ثبت و سازگار تبدیل کند.

### تصمیم پیشنهادی

پیاده‌سازی موتور پیشنهاددهی Rule-based که بر اساس پاسخ‌های کاربر موارد زیر را تعیین کند:

- حداقل Core و نوع CPU.
- حداقل RAM و الگوی چینش متقارن.
- نیاز یا عدم نیاز به GPU.
- نوع Storage و RAID پیشنهادی.
- نیاز به کارت شبکه سریع‌تر یا SFP.
- انتخاب شاسی مناسب بر اساس Rack/Tower و توسعه آینده.
- انتخاب PSU با حاشیه امن.

### پیامدها

**مزایا:**

- مسیر راهنمایی به خروجی واقعی و قابل خرید تبدیل می‌شود.
- فاصله بین کاربر غیرمتخصص و کانفیگ حرفه‌ای کم می‌شود.

**معایب:**

- نیازمند تعریف Ruleهای دقیق تجاری و فنی.
- باید با دیتابیس و Validator هماهنگ بماند.
- احتمال نیاز به امتیازدهی/Rank کردن کانفیگ‌ها وجود دارد.

---

## ADR-0013 — استفاده از جدول سرورهای آماده برای پیشنهادها

**وضعیت:** Accepted
**تاریخ:** 2026-09-05

### زمینه

صفحه «پیشنهادات ما بر اساس پاسخ‌های شما» نباید پیشنهادهای ثابت و دمو نمایش دهد. لازم است سه پیشنهاد از یک کاتالوگ قابل مدیریت از سرورهای آماده انتخاب شود.

### تصمیم

جدول `Prepared_Server_Offers` به دیتابیس اضافه شد. API جدید `api/recommend_servers.php` پاسخ‌های مسیر راهنمایی را به نیاز فنی نرمال‌شده تبدیل می‌کند و از این جدول سه پیشنهاد انتخاب می‌کند:

- اقتصادی: کمترین کانفیگ آماده‌ای که نیاز را پوشش دهد.
- مدیریت‌شده: کانفیگ متعادل با منابع و فضای توسعه بیشتر.
- پیشرفته: کانفیگ قوی‌تر/نسل جدیدتر با ظرفیت نزدیک به دو برابر نیاز در صورت موجود بودن.

### پیامدها

**مزایا:**

- پیشنهادها از دیتابیس و قابل مدیریت هستند.
- صفحه جزئیات سرور از کانفیگ واقعی همان پیشنهاد پر می‌شود.
- کانفیگ انتخابی برای ثبت پیش‌فاکتور به همان ساختار `currentConfig` تبدیل می‌شود.

**معایب:**

- کیفیت پیشنهادها به کامل بودن داده‌های جدول آماده وابسته است.
- Rule انتخاب اقتصادی/مدیریت‌شده/پیشرفته باید در آینده با منطق فروش و معماری دقیق‌تر شود.

---

## تصمیمات آینده که باید ثبت شوند

- انتخاب یا عدم انتخاب Framework فرانت‌اند.
- انتخاب ابزار Build برای CSS/JS.
- اجرای Migration جداگانه‌ی `database/migrations/2026_09_06_create_prepared_server_offers.sql` در محیط‌های موجود، پیش از فعال‌سازی پیشنهادهای آماده.
- طراحی پنل مدیریت قطعات و قیمت‌ها.
- انتقال Draftها از `localStorage` به ذخیره سمت سرور.
- مدل‌سازی دقیق‌تر RAID/Controller/Backplane/NVMe.
- تولید PDF واقعی و قابل آرشیو.
- استاندارد احراز هویت و دسترسی‌ها.

---

## ADR-0014 — اتصال چت هوشمند به سرویس OpenAI-compatible

**وضعیت:** Accepted
**تاریخ:** 2026-09-06

### تصمیم

Endpoint `api/ai_chat.php` به یک سرویس OpenAI-compatible وصل می‌شود، اما کلید و آدرس واقعی فقط از Environment Variable یا `config/ai.local.php` خوانده می‌شوند و داخل Git ذخیره نمی‌شوند.

### محدودسازی داده ارسالی

به جای ارسال کل دیتابیس یا کل State، فقط این داده‌ها ارسال می‌شوند:

- مرحله و صفحه فعلی کاربر
- هدف محاسبه‌شده و پاسخ‌های راهنمایی به شکل خلاصه
- قطعات منتخب با فیلدهای whitelist شده
- خطاها/هشدارهای Validator به صورت خلاصه
- چند گزینه سازگار و سه پیشنهاد آماده از دیتابیس

### پیامدها

- پاسخ AI به مرحله فعلی و داده واقعی سیستم نزدیک‌تر است.
- ریسک نشت داده و گیج شدن مدل با دیتای زیاد کمتر می‌شود.
- بدون تنظیم `AI_API_KEY` و `AI_API_URL`، چت خطای تنظیمات می‌دهد و پاسخ آماده/جعلی تولید نمی‌کند.
