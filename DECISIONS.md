# DECISIONS.md — دفترچه تصمیمات معماری (ADR)

این فایل تصمیمات فنی و معماری مهم پروژه را ثبت می‌کند تا دلیل وضعیت فعلی پروژه در آینده قابل پیگیری باشد.

---

## فهرست تصمیمات

| شناسه | عنوان | وضعیت |
|---|---|---|
| [ADR-0001](#adr-0001--استفاده-از-php-ساده-و-vanilla-javascript) | استفاده از PHP ساده و Vanilla JavaScript | Accepted |
| [ADR-0002](#adr-0002--نگهداری-کاتالوگ-قطعات-در-mysqlmariadb) | نگهداری کاتالوگ قطعات در MySQL/MariaDB | Accepted |
| [ADR-0003](#adr-0003--دریافت-مرحله‌ای-دادهها-بر-اساس-شاسی) | دریافت مرحله‌ای داده‌ها بر اساس شاسی | Accepted |
| [ADR-0004](#adr-0004--استفاده-از-ستونهای-json-برای-قوانین-سازگاری) | استفاده از ستون‌های JSON برای قوانین سازگاری | Accepted |
| [ADR-0005](#adr-0005--اعتبارسنجی-دو-لایه-در-فرانتاند-و-بکاند) | اعتبارسنجی دو لایه در فرانت‌اند و بک‌اند | Accepted |
| [ADR-0006](#adr-0006--ذخیره-کانفیگهای-ناتمام-در-localstorage) | ذخیره کانفیگ‌های ناتمام در localStorage | Accepted |
| [ADR-0007](#adr-0007--حذف-tailwind-cdn-و-استفاده-از-css-لوکال) | حذف Tailwind CDN و استفاده از CSS لوکال | Accepted |
| [ADR-0008](#adr-0008--محاسبه-ظرفیت-قابل-استفاده-storage-بر-اساس-raid) | محاسبه ظرفیت قابل استفاده Storage بر اساس RAID | Accepted |
| [ADR-0009](#adr-0009--ثبت-پیشفاکتور-با-کد-رهگیری-مستقل) | ثبت پیش‌فاکتور با کد رهگیری مستقل | Accepted |
| [ADR-0010](#adr-0010--تفکیک-تدریجی-javascript) | تفکیک تدریجی JavaScript | Accepted / Ongoing |
| [ADR-0011](#adr-0011--تنظیم-امن-دیتابیس-و-secretها) | تنظیم امن دیتابیس و Secretها | Accepted |
| [ADR-0012](#adr-0012--موتور-پیشنهاددهی-مسیر-راهنمایی) | موتور پیشنهاددهی مسیر راهنمایی | Accepted |
| [ADR-0013](#adr-0013--جدول-سرورهای-آماده-و-موجودی) | جدول سرورهای آماده و موجودی | Accepted |
| [ADR-0014](#adr-0014--اتصال-چت-هوشمند-به-ai-openai-compatible) | اتصال چت هوشمند به AI OpenAI-compatible | Accepted |
| [ADR-0015](#adr-0015--quick-replies-و-actionهای-قابل-اعمال-ai) | Quick Replies و Actionهای قابل اعمال AI | Accepted |
| [ADR-0016](#adr-0016--migration-جداگانه-برای-دیتابیسهای-موجود) | Migration جداگانه برای دیتابیس‌های موجود | Accepted |

---

## ADR-0001 — استفاده از PHP ساده و Vanilla JavaScript

**وضعیت:** Accepted
**تاریخ:** 2026-09-05

### زمینه

پروژه باید روی هاست‌های ساده PHP قابل اجرا باشد و نیازمند build پیچیده یا framework سمت سرور/کلاینت نباشد.

### تصمیم

- Backend با PHP ساده و PDO نوشته می‌شود.
- Frontend با Vanilla JavaScript در `assets/main.js` مدیریت می‌شود.
- HTML اصلی در `index.php` نگهداری می‌شود.

### پیامدها

**مزایا:** اجرای ساده، وابستگی کم، مناسب برای هاست اشتراکی.
**معایب:** با بزرگ شدن پروژه، نگهداری `main.js` دشوارتر می‌شود و نیاز به ماژولار شدن تدریجی دارد.

---

## ADR-0002 — نگهداری کاتالوگ قطعات در MySQL/MariaDB

**وضعیت:** Accepted
**تاریخ:** 2026-09-05

### زمینه

کاتالوگ قطعات HPE، قوانین سازگاری، قیمت، توان و وضعیت پیشنهادها باید قابل Query و نگهداری باشد.

### تصمیم

داده‌ها در MySQL/MariaDB نگهداری می‌شوند و فایل `falnicc1_server_configurator.sql` اسکیمای کامل و seed اولیه را فراهم می‌کند.

### پیامدها

- Query قطعات سازگار ساده‌تر است.
- برای تغییرات دیتابیس باید Dump و Migration جداگانه به‌روز شوند.
- در آینده بهتر است پنل مدیریت قطعات/قیمت/موجودی اضافه شود.

---

## ADR-0003 — دریافت مرحله‌ای داده‌ها بر اساس شاسی

**وضعیت:** Accepted
**تاریخ:** 2026-09-05

### زمینه

همه قطعات با همه شاسی‌ها سازگار نیستند و بارگذاری کل دیتابیس در شروع UI ضروری نیست.

### تصمیم

- `GET api/get_data.php` بدون `chassis_id` فقط شاسی‌ها را برمی‌گرداند.
- پس از انتخاب شاسی، `GET api/get_data.php?chassis_id=<id>` قطعات سازگار همان شاسی را برمی‌گرداند.

### پیامدها

- حجم پاسخ اولیه کمتر است.
- تغییر شاسی نیازمند Reset یا بازسازی بخشی از State است.
- AI actionها در صورت نیاز می‌توانند قطعات سازگار شاسی را دوباره Load کنند.

---

## ADR-0004 — استفاده از ستون‌های JSON برای قوانین سازگاری

**وضعیت:** Accepted
**تاریخ:** 2026-09-05

### زمینه

قوانینی مانند `storage_rules`, `cooling_rules`, `compatible_chassis_ids` و `compatible_cpu_ids` منعطف هستند.

### تصمیم

از ستون‌های JSON برای قوانین منعطف استفاده می‌شود و APIها این فیلدها را Decode می‌کنند.

### پیامدها

- تغییر Ruleهای ساده بدون تغییر فوری اسکیمای اصلی ممکن است.
- Queryهای JSON پیچیده‌تر هستند.
- JSON seedها باید معتبر نگه داشته شوند.

---

## ADR-0005 — اعتبارسنجی دو لایه در فرانت‌اند و بک‌اند

**وضعیت:** Accepted
**تاریخ:** 2026-09-05، به‌روزرسانی 2026-09-06

### زمینه

کاربر باید هنگام انتخاب قطعات سریع بازخورد بگیرد، ولی داده مرورگر قابل اعتماد نیست.

### تصمیم

- فرانت‌اند در `validator.runChecks()` بازخورد لحظه‌ای می‌دهد.
- بک‌اند در `api/submit_config.php` قبل از ثبت، قطعات را از دیتابیس می‌خواند و اعتبارسنجی می‌کند.
- اعتبارسنجی سمت سرور برای RAID، ظرفیت Storage، Bay/Controller، RAM Slot، PCIe/Riser، GPU و PSU تقویت شده است.

### پیامدها

- UX بهتر و ثبت امن‌تر.
- هر Rule جدید باید در هر دو لایه یا با قرارداد مشترک هماهنگ شود.

---

## ADR-0006 — ذخیره کانفیگ‌های ناتمام در localStorage

**وضعیت:** Accepted
**تاریخ:** 2026-09-05

### زمینه

کاربر ممکن است فرآیند کانفیگ را نیمه‌کاره رها کند.

### تصمیم

Draftها و Completedهای محلی در `localStorage` با کلید `falnic_server_sessions` ذخیره می‌شوند.

### پیامدها

- بازیابی سریع روی همان مرورگر.
- بین دستگاه‌ها Sync نمی‌شود.
- در آینده ذخیره سمت سرور و Login می‌تواند اضافه شود.

---

## ADR-0007 — حذف Tailwind CDN و استفاده از CSS لوکال

**وضعیت:** Accepted
**تاریخ:** 2026-09-05

### زمینه

کاربر خواست هیچ وابستگی خارجی runtime وجود نداشته باشد، ظاهر حفظ شود و CSS به شکل استاندارد داخل پروژه باشد.

### تصمیم

- Tailwind CDN حذف شد.
- Utilityهای لازم و استایل‌های اختصاصی در `assets/style.css` پیاده‌سازی شدند.
- فونت از `assets/falnic-font.woff2` خوانده می‌شود.
- لوگوهای فالنیک و HPE به صورت SVG لوکال نگهداری می‌شوند.

### پیامدها

- رندر UI وابسته به اینترنت/CDN نیست.
- هر کلاس جدید باید در CSS لوکال وجود داشته باشد.
- تست CSS coverage باید بعد از تغییرات UI انجام شود.

---

## ADR-0008 — محاسبه ظرفیت قابل استفاده Storage بر اساس RAID

**وضعیت:** Accepted
**تاریخ:** 2026-09-05، به‌روزرسانی 2026-09-06

### زمینه

ظرفیت خام دیسک با ظرفیت قابل استفاده بعد از RAID متفاوت است.

### تصمیم

فرانت‌اند و بک‌اند ظرفیت قابل استفاده را برای RAIDهای رایج محاسبه می‌کنند:

| RAID | ظرفیت قابل استفاده تقریبی |
|---|---|
| None/JBOD | `qty * capacity` |
| RAID 1 | `capacity` برای دو دیسک |
| RAID 5 | `(qty - 1) * capacity` |
| RAID 6 | `(qty - 2) * capacity` |
| RAID 10 | `(qty / 2) * capacity` |
| RAID 50 | `(qty - 2) * capacity` |
| RAID 60 | `(qty - 4) * capacity` |

### پیامدها

- کاربر ظرفیت واقعی‌تری می‌بیند.
- بک‌اند از ثبت RAID نامعتبر جلوگیری می‌کند.
- جزئیات دقیق Vendor/Controller همچنان می‌تواند در آینده کامل‌تر شود.

---

## ADR-0009 — ثبت پیش‌فاکتور با کد رهگیری مستقل

**وضعیت:** Accepted
**تاریخ:** 2026-09-05

### زمینه

کاربر پس از ثبت به کد قابل پیگیری نیاز دارد.

### تصمیم

`api/submit_config.php` کد `HPE-XXXXXX` تولید و در ستون `session_id` جدول `User_Configurations` ذخیره می‌کند.

### پیامدها

- کد کوتاه و قابل نمایش است.
- بهتر است در آینده ستون مستقل `tracking_code` اضافه شود.

---

## ADR-0010 — تفکیک تدریجی JavaScript

**وضعیت:** Accepted / Ongoing
**تاریخ:** 2026-09-06

### زمینه

`assets/main.js` بزرگ است، اما تغییر ناگهانی ساختار اصلی کانفیگوراتور ریسک دارد.

### تصمیم

- ساختار اصلی فعلاً حفظ می‌شود.
- helperهای امنیتی به `assets/js/security.js` منتقل شدند.
- ماژولار شدن بیشتر باید تدریجی و بدون شکستن رفتار فعلی انجام شود.

### مسیر آینده پیشنهادی

```text
assets/js/state.js
assets/js/api.js
assets/js/session-manager.js
assets/js/validators.js
assets/js/guidance-wizard.js
assets/js/pro-wizard.js
assets/js/configurator.js
assets/js/smart-assistant.js
```

---

## ADR-0011 — تنظیم امن دیتابیس و Secretها

**وضعیت:** Accepted
**تاریخ:** 2026-09-06

### زمینه

وجود credential در APIها برای Production امن نیست.

### تصمیم

- اتصال دیتابیس فقط از `config/database.php` و `databaseConnection()` ساخته می‌شود.
- `config/database.local.php` برای override جداگانه دیتابیس قابل استفاده است.
- `config/secrets.local.php` برای نگهداری یکپارچه همه رمزها اضافه شد.
- فایل‌های local در `.gitignore` هستند.
- ENVهای پشتیبانی‌شده:

```text
DB_HOST
DB_NAME
DB_USER
DB_PASS
DB_CHARSET
```

### پیامدها

- Secretها در Git ذخیره نمی‌شوند.
- استقرار روی هاست اشتراکی با فایل local ساده است.
- فایل نمونه `config/secrets.local.example.php` جایگزین امن برای مستندسازی ساختار Secretهاست.

---

## ADR-0012 — موتور پیشنهاددهی مسیر راهنمایی

**وضعیت:** Accepted
**تاریخ:** 2026-09-06

### زمینه

مسیر راهنمایی باید خروجی واقعی و قابل خرید بدهد، نه کارت‌های ثابت دمو.

### تصمیم

`api/recommend_servers.php` پاسخ‌های کاربر و Target را نرمال می‌کند و از جدول `Prepared_Server_Offers` سه پیشنهاد انتخاب می‌کند. Ruleها موارد زیر را لحاظ می‌کنند:

- Workload مانند DB، Virtualization، AI، Web و Storage.
- تعداد کاربران و سطح Performance.
- نیاز GPU.
- رشد آینده.
- ظرفیت Storage.
- نیاز شبکه.
- امتیاز توسعه‌پذیری و نسل.
- موجودی و زمان تأمین.

### پیامدها

- مسیر راهنمایی خروجی واقعی‌تر دارد.
- کیفیت پیشنهادها به کیفیت seed و داده موجودی وابسته است.

---

## ADR-0013 — جدول سرورهای آماده و موجودی

**وضعیت:** Accepted
**تاریخ:** 2026-09-06

### زمینه

سه پیشنهاد اقتصادی، مدیریت‌شده و پیشرفته باید از دیتابیس قابل مدیریت باشند.

### تصمیم

جدول `Prepared_Server_Offers` اضافه شد و سه seed اصلی دارد. ستون‌های موجودی هم اضافه شدند:

```text
stock_status
stock_qty
lead_time_days
```

`selected_components` ساختار قطعات پیشنهاد آماده را نگه می‌دارد تا در فرانت‌اند به `currentConfig` تبدیل شود.

### پیامدها

- صفحه پیشنهادها و Smart Assistant از داده واقعی‌تر استفاده می‌کنند.
- مدیریت دقیق موجودی در آینده نیازمند پنل مدیریت است.

---

## ADR-0014 — اتصال چت هوشمند به AI OpenAI-compatible

**وضعیت:** Accepted
**تاریخ:** 2026-09-06

### زمینه

پاسخ آماده داخلی برای دستیار هوشمند کافی نبود و باید به سرویس AI واقعی وصل می‌شد.

### تصمیم

- `api/ai_chat.php` به سرویس OpenAI-compatible متصل می‌شود.
- تنظیمات از `config/ai.php` خوانده می‌شود.
- کلید و endpoint واقعی از ENV، `config/ai.local.php` یا `config/secrets.local.php` خوانده می‌شوند.
- پاسخ آماده/جعلی حذف شده است؛ اگر AI تنظیم نباشد، خطای تنظیمات برمی‌گردد.

### محدودسازی داده ارسالی

فقط داده‌های whitelist شده ارسال می‌شوند:

- مرحله و صفحه فعلی.
- Target فنی.
- قطعات منتخب با فیلدهای ضروری.
- هشدارها/خطاهای Validator.
- چند گزینه سازگار از دیتابیس.
- سه پیشنهاد آماده و وضعیت موجودی.
- history کوتاه چت.

### پیامدها

- پاسخ‌ها به وضعیت واقعی کاربر نزدیک‌تر هستند.
- ریسک ارسال داده اضافه و Secret کاهش یافته است.
- کیفیت پاسخ به prompt و کیفیت Provider وابسته است.

---

## ADR-0015 — Quick Replies و Actionهای قابل اعمال AI

**وضعیت:** Accepted
**تاریخ:** 2026-09-06

### زمینه

کاربر باید بتواند زیر پاسخ AI روی گزینه‌هایی مثل «بله، اول CPU و رم را اصلاح کن» کلیک کند و تغییر واقعاً روی کانفیگ اعمال شود.

### تصمیم

پاسخ AI ساختار زیر را پشتیبانی می‌کند:

```json
{
  "reply": "متن کوتاه",
  "question": "سؤال بعدی",
  "quick_replies": [],
  "actions": []
}
```

Quick Reply می‌تواند فقط پیام ارسال کند یا همراه action باشد. Actionها فقط از whitelist مجاز اجرا می‌شوند:

```text
set_cpu
set_ram
set_cpu_ram
set_ram_qty
set_ram_total
set_cpu_qty
set_psu_qty
add_drive_raid10
```

برای اصلاح CPU/RAM، action ترکیبی `set_cpu_ram` اضافه شد. اگر AI فقط پیام بدهد، فرانت‌اند پیام را ارسال می‌کند و action برگشتی را به‌صورت کنترل‌شده اعمال می‌کند.

### پیامدها

- دکمه‌های AI از حالت صرفاً مکالمه‌ای خارج شده و قابلیت اعمال واقعی دارند.
- ریسک اجرای آزاد کاهش می‌یابد چون فقط actionهای whitelist شده اجرا می‌شوند.
- هر action جدید باید هم در سرور sanitize و هم در فرانت‌اند پیاده‌سازی شود.

---

## ADR-0016 — Migration جداگانه برای دیتابیس‌های موجود

**وضعیت:** Accepted
**تاریخ:** 2026-09-06

### زمینه

در محیط Production ممکن است دیتابیس قبلاً وجود داشته باشد و فقط فایل‌های پروژه جایگزین شوند.

### تصمیم

علاوه بر Dump کامل، Migration جداگانه نگهداری می‌شود:

```text
database/migrations/2026_09_06_create_prepared_server_offers.sql
```

این Migration جدول/ستون‌های پیشنهاد آماده و seedهای آن را برای دیتابیس موجود آماده می‌کند.

### پیامدها

- Deploy روی دیتابیس موجود امن‌تر و قابل کنترل‌تر است.
- باید با `--default-character-set=utf8mb4` اجرا شود تا emoji/متن فارسی مشکل نداشته باشد.

---

## تصمیمات آینده که باید ثبت شوند

- انتخاب یا عدم انتخاب Build Tool برای CSS/JS.
- ماژولار کردن کامل `assets/main.js`.
- پنل مدیریت قطعات، قیمت‌ها، موجودی و پیشنهادهای آماده.
- انتقال Draftها از `localStorage` به ذخیره سمت سرور.
- مدل‌سازی دقیق‌تر RAID/Controller/Backplane/NVMe.
- تولید PDF واقعی و قابل آرشیو.
- احراز هویت و سطح دسترسی مدیریت.
- تعریف CI و تست خودکار API/UI.
- سیاست Logging امن برای خطاهای AI و دیتابیس.
